"""
Package containig the "top level" Atomic Parameters that can be simulated / retrieved with this program.
At the moment the only fully tested is the spectra simulation functionality
"""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))